---
phase: 00-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - .nvmrc
  - .gitignore
  - .env.example
  - next.config.ts
  - tsconfig.json
  - postcss.config.mjs
  - tailwind.config.ts
  - eslint.config.mjs
  - .prettierrc.json
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - README.md
autonomous: false
requirements:
  - INFRA-01
must_haves:
  truths:
    - "Running `pnpm dev` starts Next.js on http://localhost:3000 without errors"
    - "The page at / renders the Hebrew text 'שלום עולם'"
    - "The HTML root element has dir='rtl' and lang='he' attributes"
    - "Running `pnpm tsc --noEmit` exits 0 (TypeScript strict mode passes)"
    - "Running `pnpm lint` exits 0"
  artifacts:
    - path: package.json
      provides: "Project manifest with scripts: dev, build, lint, tsc, test, playwright"
      contains: "\"dev\": \"next dev\""
    - path: app/layout.tsx
      provides: "Root layout with RTL Hebrew document direction"
      contains: "dir=\"rtl\""
    - path: app/page.tsx
      provides: "Hebrew placeholder home page"
      contains: "שלום עולם"
    - path: tsconfig.json
      provides: "TypeScript strict config"
      contains: "\"strict\": true"
    - path: tailwind.config.ts
      provides: "Tailwind v3 config wired to app/ and components/"
    - path: app/globals.css
      provides: "Tailwind directives + base RTL-aware styles"
      contains: "@tailwind base"
  key_links:
    - from: app/layout.tsx
      to: app/globals.css
      via: "import './globals.css'"
      pattern: "import.*globals\\.css"
    - from: app/layout.tsx
      to: "html element"
      via: "dir + lang attributes"
      pattern: "<html[^>]*dir=\"rtl\"[^>]*lang=\"he\""
---

<objective>
Bootstrap the Next.js 15 + React 19 + TypeScript 5 strict repo scaffold, wire Tailwind CSS, configure ESLint + Prettier, and ship a Hebrew RTL placeholder page (שלום עולם) that proves the toolchain renders Hebrew right-to-left at localhost:3000.

Purpose: Without a working dev server rendering RTL Hebrew, no other phase can begin. INFRA-01 is the gateway requirement — it proves the toolchain is operational. This plan also establishes every config file (tsconfig, tailwind, eslint, prettier) that all subsequent phases depend on.

Output: A runnable Next.js app with `pnpm dev` opening localhost:3000 showing שלום עולם in RTL layout, plus all root configs (package.json, tsconfig.json, tailwind, eslint, prettier).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/00-foundation/00-CONTEXT.md
@CLAUDE.md
@IMPLEMENTATION_PLAN.md

<interfaces>
<!-- Phase 0 is bootstrap — no prior interfaces. This plan creates the contracts subsequent phases consume. -->
<!-- Reference values that must appear in package.json: -->

Required `package.json` scripts (verbatim — Phase 1+ depend on these names):
```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "tsc": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "playwright": "playwright test"
  }
}
```

Required `tsconfig.json` compiler options:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": false,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": { "@/*": ["./*"] }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

Required pinned dependency versions (Next.js 15 + React 19 stack):
- `next@^15.0.0`
- `react@^19.0.0`
- `react-dom@^19.0.0`
- `typescript@^5.4.0`
- `tailwindcss@^3.4.0`
- `@types/node`, `@types/react`, `@types/react-dom`
- `eslint@^9`, `eslint-config-next@^15`
- `prettier@^3`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 15 + React 19 + TypeScript strict project with all root configs</name>
  <files>package.json, pnpm-lock.yaml, .nvmrc, .gitignore, .env.example, next.config.ts, tsconfig.json, postcss.config.mjs, tailwind.config.ts, eslint.config.mjs, .prettierrc.json</files>
  <read_first>
    - CLAUDE.md (entire file — tech stack, code style, commands)
    - .planning/phases/00-foundation/00-CONTEXT.md (decisions D-01 through D-05, claude's discretion)
    - IMPLEMENTATION_PLAN.md §5 Phase 0 (file list canonical)
    - package.json (if it exists — currently does not, blank repo)
  </read_first>
  <action>
    Initialize a Next.js 15 App Router project with React 19 and TypeScript strict. Use pnpm as the package manager.

    **Step 1 — Create `package.json`:**
    ```json
    {
      "name": "school-gantt",
      "version": "0.1.0",
      "private": true,
      "packageManager": "pnpm@9.12.0",
      "engines": { "node": ">=20.11" },
      "scripts": {
        "dev": "next dev",
        "build": "next build",
        "start": "next start",
        "lint": "next lint",
        "tsc": "tsc --noEmit",
        "test": "vitest run",
        "test:watch": "vitest",
        "test:coverage": "vitest run --coverage",
        "playwright": "playwright test",
        "format": "prettier --write ."
      },
      "dependencies": {
        "next": "^15.0.0",
        "react": "^19.0.0",
        "react-dom": "^19.0.0"
      },
      "devDependencies": {
        "@types/node": "^20.11.0",
        "@types/react": "^19.0.0",
        "@types/react-dom": "^19.0.0",
        "typescript": "^5.4.0",
        "eslint": "^9.0.0",
        "eslint-config-next": "^15.0.0",
        "@eslint/eslintrc": "^3.0.0",
        "prettier": "^3.3.0",
        "prettier-plugin-tailwindcss": "^0.6.0",
        "tailwindcss": "^3.4.0",
        "postcss": "^8.4.0",
        "autoprefixer": "^10.4.0"
      }
    }
    ```
    Note: vitest, playwright, shadcn deps are added in Plan 02. Test scripts are declared now so all phases see the same script names; they will fail until Plan 02 installs the runners.

    **Step 2 — Create `.nvmrc`:** single line `20.11.0`

    **Step 3 — Create `.gitignore`:**
    ```
    node_modules/
    .next/
    out/
    build/
    coverage/
    playwright-report/
    test-results/
    .env
    .env.local
    .env.*.local
    *.log
    .DS_Store
    .vscode/
    .idea/
    next-env.d.ts
    ```

    **Step 4 — Create `.env.example`** (no values — names only, per claude's discretion in CONTEXT.md):
    ```
    NEXT_PUBLIC_SUPABASE_URL=
    NEXT_PUBLIC_SUPABASE_ANON_KEY=
    SUPABASE_SERVICE_ROLE_KEY=
    RESEND_API_KEY=
    NEXT_PUBLIC_APP_URL=http://localhost:3000
    ```

    **Step 5 — Create `next.config.ts`:**
    ```ts
    import type { NextConfig } from "next";
    const nextConfig: NextConfig = {
      reactStrictMode: true,
      experimental: { typedRoutes: true },
    };
    export default nextConfig;
    ```

    **Step 6 — Create `tsconfig.json`** verbatim from the `<interfaces>` block in this plan's context (do NOT modify).

    **Step 7 — Create `postcss.config.mjs`:**
    ```js
    export default {
      plugins: { tailwindcss: {}, autoprefixer: {} },
    };
    ```

    **Step 8 — Create `tailwind.config.ts`:**
    ```ts
    import type { Config } from "tailwindcss";
    const config: Config = {
      content: [
        "./app/**/*.{ts,tsx}",
        "./components/**/*.{ts,tsx}",
        "./lib/**/*.{ts,tsx}",
      ],
      theme: { extend: {} },
      plugins: [],
    };
    export default config;
    ```

    **Step 9 — Create `eslint.config.mjs`** (flat config, ESLint 9):
    ```js
    import { FlatCompat } from "@eslint/eslintrc";
    const compat = new FlatCompat({ baseDirectory: import.meta.dirname });
    export default [
      ...compat.extends("next/core-web-vitals", "next/typescript"),
      {
        rules: {
          "@typescript-eslint/no-explicit-any": "error",
          "@typescript-eslint/no-unused-vars": ["error", { argsIgnorePattern: "^_" }],
        },
      },
    ];
    ```

    **Step 10 — Create `.prettierrc.json`:**
    ```json
    {
      "semi": true,
      "singleQuote": false,
      "trailingComma": "all",
      "printWidth": 100,
      "tabWidth": 2,
      "plugins": ["prettier-plugin-tailwindcss"]
    }
    ```

    **Step 11 — Run `pnpm install`** to generate `pnpm-lock.yaml` and `node_modules/`. This must succeed before moving on.
  </action>
  <acceptance_criteria>
    - `package.json` exists at repo root
    - `package.json` contains the literal string `"dev": "next dev"`
    - `package.json` contains the literal string `"tsc": "tsc --noEmit"`
    - `package.json` contains `"next": "^15` (Next 15 pinned)
    - `package.json` contains `"react": "^19` (React 19 pinned)
    - `tsconfig.json` exists and contains `"strict": true`
    - `tsconfig.json` contains `"@/*": ["./*"]` (path alias)
    - `tailwind.config.ts` exists and contains `"./app/**/*.{ts,tsx}"`
    - `eslint.config.mjs` exists and contains `"@typescript-eslint/no-explicit-any": "error"`
    - `.gitignore` contains the line `.env`
    - `.env.example` contains `NEXT_PUBLIC_SUPABASE_URL=`
    - `pnpm-lock.yaml` exists at repo root (proves `pnpm install` succeeded)
    - `node_modules/next` directory exists
    - Running `pnpm tsc --noEmit` exits 0 (no TS files yet, but config is valid)
  </acceptance_criteria>
  <verify>
    <automated>pnpm install &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <done>All 11 root config files exist with the exact contents specified, pnpm install completes without errors, pnpm-lock.yaml is committed, pnpm tsc --noEmit exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Create RTL Hebrew root layout, placeholder home page, and global Tailwind styles</name>
  <files>app/layout.tsx, app/page.tsx, app/globals.css, README.md</files>
  <read_first>
    - .planning/phases/00-foundation/00-CONTEXT.md (specifically: "The placeholder page should say שלום עולם" and integration points)
    - CLAUDE.md (RTL constraint: logical properties only, never hardcode left/right; html dir="rtl" lang="he")
    - tailwind.config.ts (just created — confirms content paths)
    - tsconfig.json (just created — confirms paths alias @/*)
  </read_first>
  <action>
    Create the App Router root layout, the home page rendering Hebrew "שלום עולם", and the global stylesheet with Tailwind directives.

    **Step 1 — Create `app/globals.css`:**
    ```css
    @tailwind base;
    @tailwind components;
    @tailwind utilities;

    /* RTL-aware base. Use logical properties everywhere downstream. */
    html {
      -webkit-text-size-adjust: 100%;
      tab-size: 4;
    }

    body {
      font-family: ui-sans-serif, system-ui, -apple-system, "Segoe UI",
        "Helvetica Neue", "Noto Sans Hebrew", "Arial Hebrew", sans-serif;
    }

    /* Placeholder for shadcn RTL overrides — populated in Plan 02 Task 3. */
    [dir="rtl"] {
      /* shadcn overrides go here in Plan 02 Task 3 */
    }
    ```

    **Step 2 — Create `app/layout.tsx`** (Server Component — no "use client"):
    ```tsx
    import type { Metadata } from "next";
    import "./globals.css";

    export const metadata: Metadata = {
      title: "School Gantt",
      description: "Multi-tenant school event calendar",
    };

    export default function RootLayout({
      children,
    }: {
      children: React.ReactNode;
    }) {
      return (
        <html dir="rtl" lang="he">
          <body className="min-h-screen bg-white text-neutral-900 antialiased">
            {children}
          </body>
        </html>
      );
    }
    ```

    **Step 3 — Create `app/page.tsx`** (Server Component):
    ```tsx
    export default function HomePage() {
      return (
        <main className="flex min-h-screen flex-col items-center justify-center p-8">
          <h1 className="text-4xl font-bold tracking-tight">שלום עולם</h1>
          <p className="mt-4 text-sm text-neutral-600">
            School Gantt Chart System — Phase 0 placeholder
          </p>
        </main>
      );
    }
    ```

    **Step 4 — Create `README.md`:**
    ```markdown
    # School Gantt Chart System

    Multi-tenant school event calendar. Hebrew RTL by default.

    ## Quickstart

    ```bash
    pnpm install
    pnpm dev          # http://localhost:3000 → שלום עולם
    pnpm tsc          # TypeScript check
    pnpm lint         # ESLint
    pnpm test         # Vitest (wired in Plan 02)
    pnpm playwright   # Playwright e2e (wired in Plan 02)
    ```

    ## Stack

    Next.js 15 (App Router) · React 19 · TypeScript 5 strict · Tailwind CSS · shadcn/ui · Supabase · Drizzle ORM · next-intl · Vitest · Playwright

    ## Project Structure

    See `CLAUDE.md` for the canonical architecture map and conventions.

    ## Status

    Phase 0: Foundation — in progress. See `.planning/ROADMAP.md`.
    ```

    Do NOT add `"use client"` to any of these files — they are Server Components.
    Do NOT use `<div style={{ marginLeft: ... }}>` or `text-left` / `text-right` — only logical Tailwind utilities (`ms-*`, `me-*`, `text-start`, `text-end`).
  </action>
  <acceptance_criteria>
    - `app/layout.tsx` exists
    - `app/layout.tsx` contains the literal string `dir="rtl"`
    - `app/layout.tsx` contains the literal string `lang="he"`
    - `app/layout.tsx` contains `import "./globals.css"`
    - `app/layout.tsx` does NOT contain the string `"use client"`
    - `app/page.tsx` exists
    - `app/page.tsx` contains the literal string `שלום עולם`
    - `app/page.tsx` does NOT contain the string `"use client"`
    - `app/globals.css` exists and contains `@tailwind base;`
    - `app/globals.css` contains `[dir="rtl"]` (placeholder block ready for Plan 02 Task 3)
    - `README.md` exists at repo root and contains the string `שלום עולם`
    - Running `pnpm tsc --noEmit` exits 0
    - Running `pnpm lint` exits 0
    - Running `pnpm build` exits 0 (the page builds for production)
  </acceptance_criteria>
  <verify>
    <automated>pnpm tsc --noEmit &amp;&amp; pnpm lint &amp;&amp; pnpm build</automated>
  </verify>
  <done>app/layout.tsx renders &lt;html dir="rtl" lang="he"&gt;, app/page.tsx renders שלום עולם, globals.css imports Tailwind, README.md documents the stack, pnpm build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification — RTL Hebrew placeholder renders correctly</name>
  <files>app/layout.tsx, app/page.tsx</files>
  <read_first>
    - app/layout.tsx (just created — confirms html has dir="rtl" lang="he")
    - app/page.tsx (just created — confirms שלום עולם is rendered)
  </read_first>
  <action>
    [Human verification step — no code changes. The user must run the dev server and visually confirm RTL rendering before Plan 02 begins.]

    The `<how-to-verify>` block below contains the exact steps the user will perform. This task does NOT modify any files; it only blocks until human approval.

    What this checkpoint validates:
    - INFRA-01 acceptance is observable (Hebrew RTL at localhost:3000)
    - The browser renders Hebrew glyphs without missing-character boxes (proves font fallback chain in globals.css works)
    - DOM has the expected dir="rtl" + lang="he" attributes (proves SSR didn't strip them)

    If verification fails (e.g. text appears LTR, or boxes show instead of Hebrew), do NOT proceed to Plan 02. The diagnostic path is:
    1. LTR direction → app/layout.tsx is missing `dir="rtl"` → re-run Task 2.
    2. Missing glyphs → font fallback in globals.css doesn't include Hebrew system font → add `"David"` or `"Frank Ruehl"` to font-family stack and retry.
    3. Console errors about hydration mismatch → likely a mismatch between SSR and client (rare in our setup); inspect the error.
  </action>
  <what-built>
    Phase 0 toolchain bootstrap is complete: Next.js 15 + React 19 + TypeScript strict + Tailwind, with a Hebrew RTL placeholder page. INFRA-01 acceptance proof.
  </what-built>
  <how-to-verify>
    1. In a terminal at the repo root, run: `pnpm dev`
    2. Wait for the message "Ready in [time]" and "Local: http://localhost:3000"
    3. Open http://localhost:3000 in any browser
    4. Verify visually:
       - The page shows the Hebrew text "שלום עולם" prominently
       - Text flows right-to-left (the שׁ character is on the right side of עולם, not the left)
       - The Hebrew text renders without missing-glyph boxes (□)
       - Open DevTools → Elements → confirm `<html dir="rtl" lang="he">`
    5. Stop the dev server (Ctrl+C)

    Expected outcome: Hebrew "שלום עולם" displayed RTL at localhost:3000 with no console errors.
  </how-to-verify>
  <verify>
    <automated>grep -q 'dir="rtl"' app/layout.tsx &amp;&amp; grep -q 'lang="he"' app/layout.tsx &amp;&amp; grep -q 'שלום עולם' app/page.tsx</automated>
  </verify>
  <done>User has confirmed (via "approved" resume signal) that localhost:3000 displays "שלום עולם" rendered right-to-left with no console errors and `<html dir="rtl" lang="he">` visible in DevTools.</done>
  <resume-signal>Type "approved" to continue, or describe any rendering issues (missing glyphs, LTR direction, console errors, etc.)</resume-signal>
</task>

</tasks>

<verification>
- `pnpm install` completes successfully
- `pnpm tsc --noEmit` exits 0
- `pnpm lint` exits 0
- `pnpm build` exits 0 (production build succeeds)
- `pnpm dev` starts on port 3000 with no errors
- Browser at localhost:3000 shows שלום עולם rendered RTL
- `<html>` element has `dir="rtl"` and `lang="he"` attributes (verifiable via grep on app/layout.tsx)
</verification>

<success_criteria>
INFRA-01 satisfied: Developer can run `pnpm dev` and see Hebrew-RTL placeholder at `localhost:3000`. The toolchain (Next.js 15 + React 19 + TS strict + Tailwind + ESLint + Prettier) is fully wired with all root configs in place. Subsequent plans can add test runners (Plan 02) and CI (Plan 03) on top of this foundation without touching these files.
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-01-SUMMARY.md` documenting:
- All 15 files created with their purposes
- The exact pinned versions in package.json
- Confirmation that `pnpm dev`, `pnpm tsc`, `pnpm lint`, `pnpm build` all succeed
- The Tailwind config content paths (so Plan 02 + 03 know where to add files)
- Note that `app/globals.css` has a placeholder `[dir="rtl"]` block awaiting Plan 02 Task 3 shadcn overrides
</output>
