---
phase: 02-event-crud-7-step-wizard
plan: 00
type: execute
wave: 0
depends_on: [01-04]
autonomous: true
requirements:
  - WIZARD-01
  - WIZARD-02
  - WIZARD-03
  - WIZARD-04
  - WIZARD-05
  - WIZARD-06
  - WIZARD-07
  - WIZARD-08
  - WIZARD-09
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
files_modified:
  - test/integration/wizard.test.ts
  - test/integration/events-api.test.ts
  - test/unit/events/create.test.ts
  - test/unit/events/approval.test.ts
  - test/unit/validations/event.test.ts
---

<objective>
Wave 0: Create failing test stubs for all Phase 2 requirements (WIZARD-01..09, ADMIN-01..03) BEFORE implementing any production code.

Purpose: Establish the test verification map so subsequent waves implement against tests, not vibes. Every requirement that has automated verification gets a stub that defines the expected behavior. No production code is written in this plan.

Output: Failing/pending test stubs for every Phase 2 requirement, organized into integration tests (events API, autosave, concurrent edit) and unit tests (event creation domain logic, Zod validation schemas). No production lib/events/ code yet.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@lib/db/schema.ts
@test/integration/setup.ts
@vitest.config.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 0.1: Create integration test stubs for event CRUD API (WIZARD-01..09)</name>
  <files>
    test/integration/wizard.test.ts,
    test/integration/events-api.test.ts
  </files>
  <read_first>
    - test/integration/setup.ts (testDb, skipIfNoTestDb pattern)
    - lib/db/schema.ts (events, event_grades, event_revisions tables)
    - .planning/REQUIREMENTS.md (WIZARD-01 through WIZARD-09)
  </read_first>
  <action>
    Create two integration test stub files using it.todo() for every requirement. Do NOT implement production code.

    **File 1: `test/integration/wizard.test.ts`** — Full wizard flow stubs (WIZARD-01..07):

    ```typescript
    import { describe, it } from "vitest";
    import { skipIfNoTestDb } from "./setup";

    describe("WIZARD-01: staff editor creates event via 7-step wizard", () => {
      it.todo("POST /api/v1/events creates draft event row with status=draft");
      it.todo("draft row is created immediately on wizard open");
      it.todo("created draft belongs to the authenticated editor's school");
    });

    describe("WIZARD-02: wizard autosaves draft on every step", () => {
      it.todo("PATCH /api/v1/events/:id with step data updates the draft");
      it.todo("partial step data (step 1 only) is persisted without error");
      it.todo("autosave returns the updated event version number");
    });

    describe("WIZARD-03: editor can resume draft from /dashboard", () => {
      it.todo("GET /api/v1/events?status=draft returns editor's incomplete drafts");
      it.todo("resumed draft contains all previously saved step data");
      it.todo("draft older than 7 days still appears in dashboard list");
    });

    describe("WIZARD-04: date picker bounded by active academic year", () => {
      it.todo("PATCH with date before academic year start_date returns 422");
      it.todo("PATCH with date after academic year end_date returns 422");
      it.todo("PATCH with date within academic year boundaries succeeds");
    });

    describe("WIZARD-05: grade multi-select respects editor scope", () => {
      it.todo("editor with grade=10 scope can select grade 10");
      it.todo("editor with grade=10 scope cannot select grade 11 (returns 403)");
      it.todo("admin can select any grade without scope restriction");
    });

    describe("WIZARD-06: Step 7 submit flips status draft to pending", () => {
      it.todo("POST /api/v1/events/:id/submit changes status from draft to pending");
      it.todo("submit writes a row to event_revisions with decision=null");
      it.todo("submitted event is not visible on public views (status=pending)");
    });

    describe("WIZARD-07: dashboard shows editor's events by status", () => {
      it.todo("GET /api/v1/events returns draft and pending events for the editor");
      it.todo("events are grouped/filterable by status field");
      it.todo("editor cannot see other editors' events");
    });

    describe("WIZARD-08: editor can soft-delete their own draft events", () => {
      it.todo("DELETE /api/v1/events/:id sets deleted_at (soft delete)");
      it.todo("soft-deleted event does not appear in GET /api/v1/events");
      it.todo("editor cannot delete another editor's event (returns 403)");
      it.todo("editor cannot delete a pending or approved event");
    });

    describe("WIZARD-09: concurrent edit version conflict detection", () => {
      it.todo("PATCH with stale If-Match version returns 409 Conflict");
      it.todo("PATCH with matching If-Match version succeeds and increments version");
      it.todo("409 response includes current version so client can warn user");
    });
    ```

    **File 2: `test/integration/events-api.test.ts`** — REST API contract stubs:

    ```typescript
    import { describe, it } from "vitest";
    import { skipIfNoTestDb } from "./setup";

    describe("Events API — RLS and auth boundaries", () => {
      it.todo("unauthenticated POST /api/v1/events returns 401");
      it.todo("editor from school A cannot PATCH event from school B (returns 404)");
      it.todo("admin can PATCH any event in their school");
    });

    describe("Events API — response shape", () => {
      it.todo("POST /api/v1/events returns camelCase event object (not snake_case)");
      it.todo("event object includes id, schoolId, status, version, createdAt");
      it.todo("event_grades join is included as grades array in event response");
    });

    describe("Events API — validation", () => {
      it.todo("POST with missing required fields returns 422 with field errors");
      it.todo("POST with title longer than 120 chars returns 422");
      it.todo("POST with empty grades array returns 422");
    });
    ```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const f=['test/integration/wizard.test.ts','test/integration/events-api.test.ts']; for (const p of f) { if (!fs.existsSync(p)) { console.error('missing',p); process.exit(1); } } console.log('ok');"</automated>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('test/integration/wizard.test.ts','utf8'); const checks=['WIZARD-01','WIZARD-02','WIZARD-03','WIZARD-04','WIZARD-05','WIZARD-06','WIZARD-07','WIZARD-08','WIZARD-09']; for (const k of checks) { if (!c.includes(k)) { console.error('missing',k); process.exit(1); } } console.log('all wizard describes found');"</automated>
  </verify>
  <acceptance_criteria>
    - `test/integration/wizard.test.ts` exists with describe blocks for WIZARD-01 through WIZARD-09
    - `test/integration/events-api.test.ts` exists with RLS + validation stubs
    - All tests use it.todo() — no failing assertions that would break CI
    - No production code created
  </acceptance_criteria>
  <done>Integration test stubs for all wizard CRUD requirements created using it.todo() pattern.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 0.2: Create unit test stubs for event domain logic and Zod validation (WIZARD-01..09)</name>
  <files>
    test/unit/events/create.test.ts,
    test/unit/events/approval.test.ts,
    test/unit/validations/event.test.ts
  </files>
  <read_first>
    - lib/db/schema.ts (events table fields: title, status, version, etc.)
    - lib/auth/scopes.ts (assertEditorScope pattern)
    - .planning/REQUIREMENTS.md (WIZARD-01..09 full text)
  </read_first>
  <action>
    Create three unit test stub files. Directories test/unit/events/ and test/unit/validations/ may not exist yet — create them.

    **File 1: `test/unit/events/create.test.ts`** — domain logic stubs:

    ```typescript
    import { describe, it } from "vitest";

    describe("createDraftEvent: creates a draft event row", () => {
      it.todo("returns an event with status=draft");
      it.todo("sets version=1 on initial create");
      it.todo("sets school_id from the authenticated user's school");
      it.todo("sets created_by_staff_id from the authenticated user");
    });

    describe("updateEventStep: autosaves a wizard step", () => {
      it.todo("updates only the fields provided (partial update)");
      it.todo("increments version on each successful update");
      it.todo("throws if event belongs to a different school (RLS)");
      it.todo("throws if event is not in draft status");
    });

    describe("submitEvent: transitions draft to pending", () => {
      it.todo("changes status from draft to pending");
      it.todo("validates all required wizard fields are present before submitting");
      it.todo("writes an event_revisions row on submit");
      it.todo("throws if event is already pending or approved");
    });

    describe("softDeleteEvent: marks event as deleted", () => {
      it.todo("sets deleted_at timestamp on the event");
      it.todo("throws if event is pending or approved (can only delete drafts)");
      it.todo("throws if caller is not the event creator (unless admin)");
    });
    ```

    **File 2: `test/unit/events/approval.test.ts`** — state machine stubs (Phase 2 admin create side):

    ```typescript
    import { describe, it } from "vitest";

    describe("approvalStateMachine: valid transitions", () => {
      it.todo("draft → pending is a valid transition (submit action)");
      it.todo("pending → approved is a valid transition (admin approve)");
      it.todo("pending → rejected is a valid transition (admin reject with reason)");
      it.todo("rejected → pending is a valid transition (editor resubmit)");
    });

    describe("approvalStateMachine: invalid transitions throw", () => {
      it.todo("draft → approved directly throws (must go through pending)");
      it.todo("approved → draft throws (no going back)");
      it.todo("rejected → approved directly throws (must resubmit first)");
    });

    describe("admin event create: auto-approved on creation", () => {
      it.todo("admin-created event has status=approved immediately");
      it.todo("admin-created event writes event_revisions with decided_by=admin");
    });
    ```

    **File 3: `test/unit/validations/event.test.ts`** — Zod schema stubs:

    ```typescript
    import { describe, it } from "vitest";

    describe("createEventSchema: Zod validation for POST /api/v1/events", () => {
      it.todo("rejects missing schoolId");
      it.todo("rejects title shorter than 1 char");
      it.todo("rejects title longer than 120 chars");
      it.todo("rejects empty grades array");
      it.todo("accepts valid minimal event payload");
    });

    describe("updateEventStepSchema: Zod validation for PATCH /api/v1/events/:id", () => {
      it.todo("accepts partial updates (only step-relevant fields)");
      it.todo("rejects responsible_text longer than 80 chars");
      it.todo("rejects requirements_richtext longer than 2000 chars");
      it.todo("rejects start_time >= end_time (time window constraint)");
    });

    describe("submitEventSchema: Zod validation for POST /api/v1/events/:id/submit", () => {
      it.todo("requires title, date, at least one grade, and event_type_id");
      it.todo("rejects if date is not a valid ISO date string");
    });
    ```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const f=['test/unit/events/create.test.ts','test/unit/events/approval.test.ts','test/unit/validations/event.test.ts']; for (const p of f) { if (!fs.existsSync(p)) { console.error('missing',p); process.exit(1); } } console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - All three unit test stub files exist
    - Directories test/unit/events/ and test/unit/validations/ exist
    - All tests use it.todo() — no assertions
    - Files cover: domain create/update/submit/delete logic, state machine transitions, Zod schemas
  </acceptance_criteria>
  <done>Unit test stubs for event domain logic and Zod validation schemas created.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 0.3: Create integration test stubs for admin management pages (ADMIN-01..03)</name>
  <files>
    test/integration/admin.test.ts
  </files>
  <read_first>
    - lib/db/schema.ts (staff_users, event_types, academic_years tables)
    - test/integration/setup.ts
    - .planning/REQUIREMENTS.md (ADMIN-01, ADMIN-02, ADMIN-03)
  </read_first>
  <action>
    Create one integration test stub file for admin management requirements.

    **File: `test/integration/admin.test.ts`**:

    ```typescript
    import { describe, it } from "vitest";
    import { skipIfNoTestDb } from "./setup";

    describe("ADMIN-01: admin manages staff users at /admin/staff", () => {
      it.todo("admin can create a new staff user (editor or admin role)");
      it.todo("admin can edit staff user full_name and role");
      it.todo("admin can deactivate a staff user (sets locked_until far in future)");
      it.todo("non-admin editor cannot access /admin/staff (returns 403)");
      it.todo("admin cannot create staff for a different school (RLS)");
    });

    describe("ADMIN-02: admin configures event types at /admin/event-types", () => {
      it.todo("admin can create a new event type with label_he, label_en, color_hex, glyph");
      it.todo("admin can edit event type label and sort_order");
      it.todo("admin can deactivate an event type (is_active=false)");
      it.todo("deactivated event type does not appear in wizard step 3 options");
      it.todo("non-admin cannot POST to event-types API (returns 403)");
    });

    describe("ADMIN-03: admin configures active academic year at /admin/year", () => {
      it.todo("admin can create an academic year with label, start_date, end_date");
      it.todo("admin can set a year as the active academic year for the school");
      it.todo("date picker in wizard uses active academic year bounds");
      it.todo("non-admin cannot modify academic year (returns 403)");
    });
    ```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); if (!fs.existsSync('test/integration/admin.test.ts')) { console.error('missing admin.test.ts'); process.exit(1); } const c=fs.readFileSync('test/integration/admin.test.ts','utf8'); for (const k of ['ADMIN-01','ADMIN-02','ADMIN-03']) { if (!c.includes(k)) { console.error('missing describe for',k); process.exit(1); } } console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `test/integration/admin.test.ts` exists
    - Contains describe blocks for ADMIN-01, ADMIN-02, ADMIN-03
    - All tests use it.todo()
    - No production code created
  </acceptance_criteria>
  <done>Integration test stubs for all admin management requirements created.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 0.4: Verify test suite still passes with new stubs added</name>
  <files></files>
  <read_first>
    - vitest.config.ts (project configuration)
  </read_first>
  <action>
    Run the full test suite to confirm the new todo stubs integrate cleanly without breaking existing tests:

    1. Run `pnpm test` to verify all existing unit tests still pass
    2. Confirm the new stubs show up as "todo" (not "failing") in test output
    3. Verify no TypeScript errors from the new files
  </action>
  <verify>
    <automated>pnpm test --reporter=verbose 2>&1 | tail -20</automated>
    <automated>pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits 0 (todo tests are not failures)
    - No TypeScript errors from new test files
    - Existing unit tests (auth/scopes, auth/session, smoke) still pass
  </acceptance_criteria>
  <done>Test suite passes with all new stubs integrated. Test map is established for Phase 2 implementation.</done>
</task>

</tasks>

<verification>
- test/integration/wizard.test.ts has describe blocks for WIZARD-01 through WIZARD-09
- test/integration/events-api.test.ts has RLS and validation stubs
- test/integration/admin.test.ts has describe blocks for ADMIN-01, ADMIN-02, ADMIN-03
- test/unit/events/create.test.ts has domain logic stubs
- test/unit/events/approval.test.ts has state machine stubs
- test/unit/validations/event.test.ts has Zod schema stubs
- pnpm test exits 0 (todo stubs are not failures in Vitest)
- No production code in lib/events/, app/api/v1/events/, or components/Wizard/
</verification>

<success_criteria>
- `ls test/integration/` shows wizard.test.ts, events-api.test.ts, admin.test.ts alongside existing files
- `ls test/unit/events/` shows create.test.ts, approval.test.ts
- `ls test/unit/validations/` shows event.test.ts
- `pnpm test` exits 0
- `grep -r "it.todo" test/integration/wizard.test.ts | wc -l` returns at least 20 stubs
</success_criteria>

<output>
After completion, create `.planning/phases/02-event-crud-7-step-wizard/02-00-SUMMARY.md` documenting:
- List of all test stub files created
- Count of it.todo() stubs per requirement area
- Confirmation that pnpm test passes
- Any deviations from plan
</output>
